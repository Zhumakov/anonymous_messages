version: "3.9"

services:
  app:
    build: .
    image: anonym-messages
    command: "pytest -v"
    depends:
      - db

  db:
    image: postgres:13
    volumes:
      - db_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    restart: always

volumes:
  db_data:
